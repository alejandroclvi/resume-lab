{
  "name": "resume-lab",
  "private": true,
  "version": "1.0.0",
  "description": "Free, open-source resume PDF editor — live PDF proofing, a CSS box-model style inspector, and a 12-column grid design canvas. Runs entirely in the browser.",
  "license": "MIT",
  "keywords": ["resume", "cv", "pdf", "editor", "react-pdf", "resume-builder", "open-source"],
  "type": "module",
  "scripts": {
    "predev": "node scripts/gen-manifest.mjs",
    "prebuild": "node scripts/gen-manifest.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@react-pdf/renderer": "^4.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-pdf": "^9.2.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
